'use strict';

const expect = require('chai').expect;

describe('Word Dicionary', () => {
    
    describe('method', () => {
        
        it('should work', () => {
            
            expect(true).to.be.true;
        })
    });
});