'use strict';

const expect = require('chai').expect;

describe('Boolean', () => {
    
    describe('method', () => {
        
        it('should work', () => {
            
            expect(true).to.be.true;
        })
    });
});